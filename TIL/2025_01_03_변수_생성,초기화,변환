변수 생성 및 초기화
  자료형 변수이름 = 값

정수형과 실수형의 형변환
  ex)float fTen = (float)iTen; // int형이었던 iTen을 float형으로 형변환
     int iTen = (int)fFive; // float형이었던 fFive를 int형으로 형변환

정수형과 실수형의 문자열 변환
  ex)string nn = n.ToString(); // 숫자를 문자열string형으로 변환

문자열의 정수형과 실수형의 형변환
  ex)int iTen = Convert.ToInt32(strTen); // string형이었던 strTen을 Convert를 사용 하여 32비트 내의 int형 변환
     float fTen = float.Parse(strTen); // string형이었던 strTen을 Parse를 사용하여 float형으로 변환

Convert 와 Parse의 차이점
  공통점
    -두가지 모두 문자열을 형변환할 때 주로 사용
    -변환 할 수 없는 타입이면 에러 발생
  차이점
      Convert
        여러 종류의 자료형을 변환 가능
        null값을 기본형으로 변환
      Parse
        string형만을 변환 가능
        변환값이 null일 경우 에러 발생
        string형만을 변환하기에 string으로 변환 불가
  문자열의 변환에 있어 Convert가 Parse보다 우선순위에 있어 밀리는 부분을 찾지 못하였습니다.
        
\" 문자열 내에 "표시
\n 한줄 내려쓰기
\t 들여쓰기
\' '표시
\\ \표시
